unison/loading README

This directory contains Makefiles which control the loading and updating
of Unison. It also controls the order of loading (for example, pseq loaded
before updating pseqsets).

General directions
==================
$ cd unison/loading
$ PGPASSWORD=<loader password> make




Specific directions
===================
.ids files
